---
"mattpocock-skills": patch
---

Add four skills to the `in-progress` bucket, model-invoked: `ux-design` (user flows, information architecture, and states from a spec), `ui-design` (turns a validated UX flow into a visual interface and component system), `experiment` (runs a controlled experiment and keeps only evidence-backed changes), and `test` (validates a finished piece of work against acceptance criteria, as distinct from `tdd`'s test-first discipline).

Skipped the other seven skills from the same import (`research`, `code-review`, `grill-with-docs`, `prototype`, `to-spec`, `to-tickets`, `implement`) because they collide by name with existing, more repo-integrated skills.
