---
"mattpocock-skills": minor
---

Add the `design-system` model-invoked engineering skill. It owns the visual rules between `ui-design` and `prototype`: semantic colour roles, type/spacing/radius/elevation scales, component visual rules and states, accessibility, dark mode, and implementation-ready design tokens, kept as the single source of truth so screens and the prototype share one visual language.

Wired into the promoted set: `.claude-plugin/plugin.json`, the top-level and `engineering/` `README.md` (Model-invoked), a docs page at `docs/engineering/design-system.md`, and a route in `ask-matt`'s Design section (the chain is now `ux-design → ui-design → design-system → prototype`).
