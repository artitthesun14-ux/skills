# 11: พื้นหลังไดนามิก + ปุ่มล็อกธีม

**What to build:** ทุกครั้งที่เจนชุดใหม่ พื้นหลังหน้าเปลี่ยนเป็นพาสเทลของ hue ที่มาจากสี Primary
ของการ์ดที่โฟกัสอยู่ โดย S/L คงที่ (6% / 95%) เพื่อให้คอนทราสต์กับ `--ink` ไม่ตกไม่ว่า hue ไหน
มีปุ่มกุญแจล็อก "ธีมพื้นหลังเท่านั้น" (คนละเรื่องกับ FR-5 ที่ล็อกสีเพื่อจัดชุด)

**Blocked by:** 09 (ต้องไม่มีชุดโทเคน dark มาแย่ง `--bg` ก่อน)

**Status:** done (เฟส 4-C)

- [x] โทเคน `--theme-tint-h/s/l` + `--bg` คำนวณจากโทเคนนั้น
- [x] ก่อนมีชุดแรก พื้นหลังยังเป็น `#FBFAF7` เดิมเป๊ะ
- [x] `ThemeLockButton` ใน header ขวาสุด ไอคอนกุญแจ มี `aria-pressed` + `aria-label` เปลี่ยนตามสถานะ
- [x] ล็อกแล้วเจนใหม่ พื้นหลังไม่ขยับ
- [x] persist `themeLocked` + tint ที่ล็อกไว้ ใน storage key เดิม
- [x] `prefers-reduced-motion` = สลับสีทันที ไม่ไล่สี
- [x] มี `say()` ทุกครั้งที่ล็อก/ปลดล็อก (กฎ no silent state change)
