# 13: ปุ่ม "ดูสีเพิ่มเติม" + มุมมองจัดกลุ่มตามโทนสี

**What to build:** แถวสวอตช์สีที่เลื่อนแนวนอนสองแถว (สีของวันนี้ และตัวเลือกสีในฟอร์มเพิ่มเสื้อผ้า)
มีปุ่ม "ดูสีเพิ่มเติม" ท้ายแถว เปิด modal ที่จัดกลุ่มสีตามโทน (ใช้ `colorNameTH()` เดิมเป็น key
ไม่ประดิษฐ์ taxonomy ใหม่) แตะกลุ่มแล้วกางเป็นทุกเฉดในกลุ่มนั้น

**Blocked by:** None (ไม่ผูกกับระบบภาพเฟส C)

**Status:** done (เฟส 4-D)

- [x] สองทางเข้าใช้แหล่งข้อมูลต่างกันจริง: (a) สีที่มีจริงในตู้ (b) `PRESET_COLORS` เต็มชุด
- [x] กลุ่มที่ไม่มีเฉดในแหล่งข้อมูลนั้นไม่แสดง
- [x] เลือกเฉด = ปิด modal + ใช้ค่าที่จุดเดิม + มีข้อความยืนยัน
- [x] `role="dialog" aria-modal="true"`, trap focus, Escape/แตะ scrim ปิดได้, คืน focus ไปปุ่มที่เปิด
- [x] ทุกสวอตช์มีชื่อสี + hex เป็นข้อความ (กฎ "ไม่สื่อด้วยสีอย่างเดียว")
